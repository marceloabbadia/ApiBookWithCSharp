@page
@model BookStore.App.Pages.Rented.CreateModel

        @{
        ViewData["Title"] = "Create";
        }
        
        <h1>Create</h1>
        
    <h4>RentedBook</h4>
    <hr />
    <div class="row">
    <div class="col-md-4">
    <form method="post">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="RentedBook.RentedAt" class="control-label"></label>
                <input asp-for="RentedBook.RentedAt" class="form-control" />
                <span asp-validation-for="RentedBook.RentedAt" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="RentedBook.ReturnedAt" class="control-label"></label>
                <input asp-for="RentedBook.ReturnedAt" class="form-control" />
                <span asp-validation-for="RentedBook.ReturnedAt" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="RentedBook.BookId" class="control-label"></label>
                <select asp-for="RentedBook.BookId" class ="form-control" asp-items="ViewBag.BookId"></select>
            </div>
            <div class="form-group">
                <label asp-for="RentedBook.UserId" class="control-label"></label>
                <select asp-for="RentedBook.UserId" class ="form-control" asp-items="ViewBag.UserId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
